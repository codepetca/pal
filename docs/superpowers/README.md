# Historical implementation notes

Files under `specs/` and `plans/` record earlier implementation sessions. They are
retained as history and are not current product or API documentation. Use the living
documents linked from the repository [README](../../README.md), and treat source code
and `packages/contract` as authoritative when a historical note disagrees.
